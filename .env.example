# Supabase (browser-safe publishable/anon key only — never put service_role in VITE_*)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Server-side only — never expose in frontend builds
SERVICE_ROLE_KEY=your-service-role-key

# Gemini key must NOT go in VITE_*. Set it as a Supabase Edge Function secret named:
#   GEMINI_API_KEY
# Dashboard → Project Settings → Edge Functions → Secrets
# Get a key: https://aistudio.google.com/apikey
# The frontend calls /functions/v1/gemini-chat.

# Base path:
# - Render custom domain (storybuilder.pw): leave unset or "/"
# - GitHub Pages (path deploy): /storybuilder/
# VITE_BASE_PATH=/

# Canonical public URL for auth email redirects (Render production):
# VITE_APP_URL=https://storybuilder.pw

# Supabase Dashboard → Authentication → URL configuration:
# - Site URL: https://storybuilder.pw/   ← NEVER http://localhost:3000
# - Redirect URLs (add all that apply):
#   https://storybuilder.pw/**
#   https://storybuilder.pw/reset-password
#   https://www.storybuilder.pw/**
#   http://localhost:5173/**
#   http://localhost:5175/**
#   https://pab89154.github.io/storybuilder/**
#   https://pab89154.github.io/storybuilder/reset-password
#
# Custom SMTP: Dashboard → Authentication → SMTP (Resend or iCloud). See supabase/SETUP.md.
